import styled from '@emotion/styled';

const Post = () => {
  return <Container>게시글</Container>;
};

const Container = styled.div`
  padding-top: 4rem;
  padding-bottom: 4rem;
  line-height: 1.5;
`;

export default Post;
